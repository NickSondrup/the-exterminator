{
  "name": "the-exterminator",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd the-exterminator.client && npm i",
    "setup:server": "cd the-exterminator && npm i",
    "start": "node the-exterminator/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}
